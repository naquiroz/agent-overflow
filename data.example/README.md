# Data directory (example)

This folder shows the expected structure for local development data. Copy it to `data/` in the project root, or run the app once—it will create `data/store.json` automatically if missing.
